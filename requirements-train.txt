# ─────────────────────────────────────────────
# APOLLO-M — FULL pipeline requirements (training).
#
# Everything needed to regenerate results: toxic-bert scoring, the embedder, the
# Temporal Fusion Transformer, clustering, the graph layer.
#
#   pip install -r requirements-train.txt
#
# requirements.txt is deliberately the SMALLER serving set, because Streamlit
# Community Cloud installs requirements.txt by name and cannot be pointed at a
# different file. Shipping torch, transformers and pytorch-forecasting to a
# dashboard that reads precomputed CSVs made every boot slow and fragile for no
# benefit -- the hosted app never imports any of them.
# ─────────────────────────────────────────────

# Core
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0

# Deep Learning
torch>=2.0.0
torchvision>=0.15.0

# NLP / Text Embeddings (BERT / RoBERTa)
transformers>=4.35.0
tokenizers>=0.15.0
datasets>=2.14.0
huggingface-hub>=0.19.0

# Time-Series Forecasting (TFT Macro Layer)
pytorch-forecasting>=1.0.0
pytorch-lightning>=2.0.0
lightning>=2.0.0

# Graph Analysis (Meso Layer)
networkx>=3.1

# Database
psycopg2-binary>=2.9.0

# Dashboard
streamlit>=1.28.0
plotly>=5.17.0

# API
fastapi>=0.104.0
uvicorn>=0.24.0
requests>=2.31.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.66.0
